<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Blog</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <%- include('../partials/_navbar') %>

    <div class="container">
        <h1 class="heading"><%= user ? user.username : 'All Blogs' %>'s Blogs</h1>

        <% if (blogs.length === 0) { %>
            <p>No blogs found.</p>
        <% } else { %>
            <% blogs.forEach(blog => { %>
            <% }); %>
        <% } %>

        <h2 class="sub-heading">Edit Blog Post</h2>
        <form action="/posts/<%= post._id %>?_method=PUT" method="POST" class="form">
            <input type="text" name="title" value="<%= post.title %>" required class="input-field">
            <textarea name="content" required class="textarea-field"><%= post.content %></textarea>
            <button type="submit" class="btn">Update Post</button>
        </form>

        <form action="/posts/<%= post._id %>?_method=DELETE" method="POST" style="display:inline;">
            <button type="submit" class="btn delete-btn">Delete Post</button>
        </form>
    </div>
</body>
</html>
