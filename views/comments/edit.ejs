<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Comment</title>
    <link rel="stylesheet" href="/styles/style.css">
</head>
<body>
    <%- include('../partials/_navbar') %>

    <div class="container">
        <h1 class="edit-comment-title">Edit Comment</h1>

        <form action="/posts/<%= postId %>/comments/<%= comment._id %>?_method=PUT" method="POST" class="edit-comment-form">
            <div class="form-group">
                <label for="content" class="comment-label">Comment:</label>
                <textarea name="content" id="content" rows="4" required class="comment-input"><%= comment.content %></textarea>
            </div>
            <button type="submit" class="small-btn">Update Comment</button>
        </form>

        <br>
        <a href="/posts/<%= postId %>" class="cancel-link">Cancel</a>
    </div>
</body>
</html>
